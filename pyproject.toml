[project]
name = "rl-2048"
version = "0.1.0"
description = "2048 game environment for reinforcement learning"
requires-python = ">=3.9"
dependencies = [
    "numpy",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
